language: c
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y debhelper dpkg-dev libssl-dev libevent-dev sqlite3 libsqlite3-dev postgresql-client libpq-dev mysql-client libmysqlclient-dev libhiredis-dev
os:
  - linux
  - osx
